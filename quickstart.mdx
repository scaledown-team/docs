# Getting Started with ScaleDown: Your AI Cost Optimization Guide

**ScaleDown Team** • March 13, 2025 • 10 min read

Welcome to ScaleDown! If you're spending too much on AI API calls and want to slash those costs without sacrificing quality, you're in the right place. This guide will walk you through everything you need to know to start optimizing your AI costs today.

---

## What is ScaleDown?

ScaleDown is an intelligent prompt compression service that reduces your AI token usage while preserving the semantic meaning of your prompts. Think of it as a smart compression tool for your AI conversations—you get the same quality responses while paying significantly less.

The bottom line: **ScaleDown can reduce your AI costs by 30-90%** depending on your compression settings.

---

## Before You Start

To use ScaleDown, you'll need:
* An API key
* Basic knowledge of making API calls
* Your existing AI prompts that you want to optimize

Ready to get your API key? [Contact our sales team](https://blog.scaledown.ai/blog/getting-started).

---

## Your First ScaleDown Request

Here's how to make your first API call to compress a prompt.

<Tabs>
  <Tab title="Python" icon="python">
    ```python
    import requests
    import json

    # ScaleDown API endpoint
    url = "[https://api.scaledown.xyz/compress/raw/](https://api.scaledown.xyz/compress/raw/)"

    # Your headers (replace YOUR_API_KEY with your actual key)
    headers = {
        'x-api-key': 'YOUR_API_KEY',
        'Content-Type': 'application/json'
    }
    ```
  </Tab>
  <Tab title="TypeScript" icon="typescript">
    ```typescript
    // ScaleDown API endpoint
    const url = "[https://api.scaledown.xyz/compress/raw/](https://api.scaledown.xyz/compress/raw/)";

    // Your headers (replace YOUR_API_KEY with your actual key)
    const headers = {
        'x-api-key': 'YOUR_API_KEY',
        'Content-Type': 'application/json'
    };
    ```
  </Tab>
  <Tab title="JavaScript" icon="javascript">
    ```javascript
    // ScaleDown API endpoint
    const url = "[https://api.scaledown.xyz/compress/raw/](https://api.scaledown.xyz/compress/raw/)";

    // Your headers (replace YOUR_API_KEY with your actual key)
    const headers = {
        'x-api-key': 'YOUR_API_KEY',
        'Content-Type': 'application/json'
    };
    ```
  </Tab>
</Tabs>
